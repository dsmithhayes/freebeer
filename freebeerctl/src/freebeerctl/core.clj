; This program will check the current working directory
; for music files and then add them to a torrent file.

(ns freebeerctl.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
